<div class="center jumbotron">
  <h1>ALPHA-BLOG</h1>
  <% if logged_in? %>
    
  <% else %>
    <%= link_to "Sign up now", signup_path, class: "btn btn-danger btn-lg btn-xlarge" %>
  <% end %>
</div>
